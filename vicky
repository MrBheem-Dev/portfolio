<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Vigneshwaran Theerthagiri — Full Stack Developer</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet"/>
<style>
  :root {
    --accent: #7c5cfc;
    --accent2: #fc5c7d;
    --accent3: #43e8d8;
    --glass: rgba(255,255,255,0.07);
    --glass-border: rgba(255,255,255,0.13);
    --glass-hover: rgba(255,255,255,0.13);
    --text: #f0eeff;
    --muted: rgba(240,238,255,0.55);
    --bg1: #0d0b1e;
    --bg2: #130f2e;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg1);
    color: var(--text);
    overflow-x: hidden;
    cursor: none;
  }

  /* Custom cursor */
  .cursor {
    position: fixed; width: 12px; height: 12px;
    background: var(--accent); border-radius: 50%;
    pointer-events: none; z-index: 9999;
    transform: translate(-50%,-50%);
    transition: transform 0.1s, background 0.2s;
    mix-blend-mode: screen;
  }
  .cursor-ring {
    position: fixed; width: 36px; height: 36px;
    border: 1.5px solid rgba(124,92,252,0.5);
    border-radius: 50%; pointer-events: none; z-index: 9998;
    transform: translate(-50%,-50%);
    transition: transform 0.18s ease, width 0.2s, height 0.2s, border-color 0.2s;
  }

  /* Animated BG */
  .bg-canvas {
    position: fixed; inset: 0; z-index: 0;
    background: radial-gradient(ellipse 80% 60% at 20% 10%, rgba(124,92,252,0.18) 0%, transparent 60%),
                radial-gradient(ellipse 60% 50% at 80% 80%, rgba(252,92,125,0.13) 0%, transparent 55%),
                radial-gradient(ellipse 50% 40% at 60% 30%, rgba(67,232,216,0.1) 0%, transparent 50%),
                linear-gradient(135deg, #0d0b1e 0%, #130f2e 100%);
  }

  .orb {
    position: fixed; border-radius: 50%; filter: blur(80px);
    pointer-events: none; z-index: 0; animation: orbFloat 12s ease-in-out infinite;
  }
  .orb1 { width: 500px; height: 500px; top: -100px; left: -100px; background: rgba(124,92,252,0.15); animation-delay: 0s; }
  .orb2 { width: 400px; height: 400px; bottom: -80px; right: -80px; background: rgba(252,92,125,0.12); animation-delay: -5s; }
  .orb3 { width: 300px; height: 300px; top: 40%; left: 50%; background: rgba(67,232,216,0.1); animation-delay: -9s; }

  @keyframes orbFloat {
    0%,100% { transform: translate(0,0) scale(1); }
    33% { transform: translate(30px,-20px) scale(1.05); }
    66% { transform: translate(-20px,30px) scale(0.95); }
  }

  /* Grid overlay */
  .grid-overlay {
    position: fixed; inset: 0; z-index: 0; pointer-events: none;
    background-image: linear-gradient(rgba(124,92,252,0.03) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(124,92,252,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  /* NAV */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    padding: 20px 60px;
    display: flex; justify-content: space-between; align-items: center;
    background: rgba(13,11,30,0.6);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--glass-border);
    transition: all 0.3s;
  }
  .nav-logo {
    font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.3rem;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    letter-spacing: -0.5px;
  }
  .nav-links { display: flex; gap: 36px; list-style: none; }
  .nav-links a {
    color: var(--muted); text-decoration: none; font-size: 0.9rem;
    font-weight: 500; letter-spacing: 0.3px;
    transition: color 0.2s;
    position: relative;
  }
  .nav-links a::after {
    content: ''; position: absolute; bottom: -4px; left: 0; right: 0; height: 1px;
    background: linear-gradient(90deg, var(--accent), var(--accent2));
    transform: scaleX(0); transition: transform 0.3s; transform-origin: left;
  }
  .nav-links a:hover { color: var(--text); }
  .nav-links a:hover::after { transform: scaleX(1); }

  /* SECTIONS */
  section { position: relative; z-index: 1; }

  /* HERO */
  #hero {
    min-height: 100vh;
    display: flex; align-items: center; justify-content: center;
    padding: 120px 60px 80px;
    text-align: center;
  }
  .hero-inner { max-width: 820px; }
  .hero-badge {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--glass); border: 1px solid var(--glass-border);
    backdrop-filter: blur(12px);
    padding: 8px 20px; border-radius: 50px;
    font-size: 0.8rem; color: var(--accent3); font-weight: 500;
    letter-spacing: 1.5px; text-transform: uppercase;
    margin-bottom: 32px;
    animation: fadeUp 0.8s ease both;
  }
  .hero-badge::before {
    content: ''; width: 6px; height: 6px; border-radius: 50%;
    background: var(--accent3); animation: pulse 2s infinite;
  }
  @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.5;transform:scale(1.3)} }

  .hero-name {
    font-family: 'Syne', sans-serif; font-weight: 800;
    font-size: clamp(2.8rem, 7vw, 5.5rem);
    line-height: 1.05; letter-spacing: -2px;
    margin-bottom: 16px;
    animation: fadeUp 0.8s 0.1s ease both;
  }
  .hero-name span {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent2) 50%, var(--accent3) 100%);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .hero-title {
    font-family: 'Syne', sans-serif; font-size: clamp(1rem, 2.5vw, 1.4rem);
    font-weight: 600; color: var(--muted); letter-spacing: 2px;
    text-transform: uppercase; margin-bottom: 28px;
    animation: fadeUp 0.8s 0.2s ease both;
  }
  .hero-desc {
    font-size: 1.05rem; color: var(--muted); line-height: 1.8;
    max-width: 560px; margin: 0 auto 48px;
    animation: fadeUp 0.8s 0.3s ease both;
  }
  .hero-cta {
    display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;
    animation: fadeUp 0.8s 0.4s ease both;
  }
  .btn-primary {
    padding: 14px 36px; border-radius: 50px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    color: #fff; font-weight: 600; font-size: 0.95rem;
    text-decoration: none; border: none; cursor: none;
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 4px 30px rgba(124,92,252,0.35);
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 40px rgba(124,92,252,0.5); }
  .btn-outline {
    padding: 14px 36px; border-radius: 50px;
    background: var(--glass); border: 1px solid var(--glass-border);
    backdrop-filter: blur(12px);
    color: var(--text); font-weight: 600; font-size: 0.95rem;
    text-decoration: none; cursor: none;
    transition: transform 0.2s, background 0.2s, border-color 0.2s;
  }
  .btn-outline:hover { transform: translateY(-2px); background: var(--glass-hover); border-color: rgba(255,255,255,0.25); }

  .hero-scroll {
    position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: 8px;
    color: var(--muted); font-size: 0.75rem; letter-spacing: 1px; text-transform: uppercase;
    animation: fadeUp 1s 0.8s ease both;
  }
  .scroll-line {
    width: 1px; height: 40px;
    background: linear-gradient(to bottom, var(--accent), transparent);
    animation: scrollAnim 2s ease-in-out infinite;
  }
  @keyframes scrollAnim { 0%,100%{transform:scaleY(1);opacity:1} 50%{transform:scaleY(0.5);opacity:0.4} }

  @keyframes fadeUp {
    from { opacity:0; transform:translateY(30px); }
    to { opacity:1; transform:translateY(0); }
  }

  /* GLASS CARD */
  .glass-card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(16px);
    border-radius: 20px;
    transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
  }
  .glass-card:hover {
    transform: translateY(-4px);
    border-color: rgba(124,92,252,0.3);
    box-shadow: 0 20px 60px rgba(124,92,252,0.12);
  }

  /* SECTION HEADER */
  .section-header {
    text-align: center; margin-bottom: 64px;
  }
  .section-tag {
    display: inline-block;
    font-size: 0.75rem; font-weight: 600; letter-spacing: 2px;
    text-transform: uppercase; color: var(--accent);
    margin-bottom: 12px;
  }
  .section-title {
    font-family: 'Syne', sans-serif; font-weight: 800;
    font-size: clamp(2rem, 4vw, 3rem); letter-spacing: -1px;
  }
  .section-title span {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  }
  .section-line {
    width: 60px; height: 3px; border-radius: 2px; margin: 16px auto 0;
    background: linear-gradient(90deg, var(--accent), var(--accent2));
  }

  /* ABOUT */
  #about { padding: 120px 60px; }
  .about-grid {
    display: grid; grid-template-columns: 1fr 1fr; gap: 40px;
    max-width: 1100px; margin: 0 auto; align-items: center;
  }
  .about-card { padding: 48px; }
  .about-text { font-size: 1.05rem; line-height: 1.85; color: var(--muted); }
  .about-text p + p { margin-top: 16px; }
  .about-text strong { color: var(--text); font-weight: 600; }
  .about-stats {
    display: grid; grid-template-columns: 1fr 1fr; gap: 20px;
  }
  .stat-card {
    padding: 28px 24px; text-align: center; border-radius: 16px;
    background: rgba(124,92,252,0.08); border: 1px solid rgba(124,92,252,0.15);
    transition: background 0.3s, border-color 0.3s;
  }
  .stat-card:hover { background: rgba(124,92,252,0.15); border-color: rgba(124,92,252,0.3); }
  .stat-num {
    font-family: 'Syne', sans-serif; font-size: 2.4rem; font-weight: 800;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    line-height: 1;
  }
  .stat-label { font-size: 0.8rem; color: var(--muted); margin-top: 6px; font-weight: 500; letter-spacing: 0.5px; }

  /* SKILLS */
  #skills { padding: 120px 60px; }
  .skills-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 16px; max-width: 1100px; margin: 0 auto;
  }
  .skill-pill {
    padding: 18px 16px; border-radius: 16px; text-align: center;
    background: var(--glass); border: 1px solid var(--glass-border);
    backdrop-filter: blur(12px);
    transition: all 0.3s; cursor: default;
    position: relative; overflow: hidden;
  }
  .skill-pill::before {
    content: ''; position: absolute; inset: 0; border-radius: 16px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    opacity: 0; transition: opacity 0.3s;
  }
  .skill-pill:hover::before { opacity: 0.1; }
  .skill-pill:hover {
    transform: translateY(-4px);
    border-color: rgba(124,92,252,0.4);
    box-shadow: 0 12px 40px rgba(124,92,252,0.15);
  }
  .skill-icon { font-size: 1.8rem; margin-bottom: 10px; display: block; position: relative; }
  .skill-name { font-size: 0.85rem; font-weight: 600; color: var(--text); position: relative; }
  .skill-type { font-size: 0.72rem; color: var(--muted); margin-top: 3px; position: relative; }

  /* PROJECTS */
  #projects { padding: 120px 60px; }
  .projects-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(480px, 1fr));
    gap: 24px; max-width: 1100px; margin: 0 auto;
  }
  .project-card { padding: 40px; border-radius: 20px; position: relative; overflow: hidden; }
  .project-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--accent), var(--accent2), var(--accent3));
    opacity: 0; transition: opacity 0.3s;
  }
  .project-card:hover::before { opacity: 1; }
  .project-num {
    font-family: 'Syne', sans-serif; font-size: 3.5rem; font-weight: 800;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    opacity: 0.3; line-height: 1; margin-bottom: 16px;
    transition: opacity 0.3s;
  }
  .project-card:hover .project-num { opacity: 0.6; }
  .project-name {
    font-family: 'Syne', sans-serif; font-size: 1.4rem; font-weight: 700;
    margin-bottom: 12px; letter-spacing: -0.3px;
  }
  .project-desc { font-size: 0.95rem; color: var(--muted); line-height: 1.75; }
  .project-tags { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 24px; }
  .tag {
    padding: 5px 14px; border-radius: 50px; font-size: 0.75rem; font-weight: 600;
    background: rgba(124,92,252,0.12); border: 1px solid rgba(124,92,252,0.2);
    color: var(--accent); letter-spacing: 0.3px;
    transition: background 0.2s, border-color 0.2s;
  }
  .project-card:hover .tag { background: rgba(124,92,252,0.2); border-color: rgba(124,92,252,0.4); }

  /* CONTACT */
  #contact { padding: 120px 60px; }
  .contact-inner { max-width: 700px; margin: 0 auto; }
  .contact-card { padding: 60px; border-radius: 24px; text-align: center; position: relative; overflow: hidden; }
  .contact-card::before {
    content: ''; position: absolute; inset: -1px; border-radius: 24px; z-index: -1;
    background: linear-gradient(135deg, rgba(124,92,252,0.3), rgba(252,92,125,0.2), rgba(67,232,216,0.15));
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude; padding: 1px;
  }
  .contact-title {
    font-family: 'Syne', sans-serif; font-size: 2rem; font-weight: 800;
    margin-bottom: 12px; letter-spacing: -0.5px;
  }
  .contact-subtitle { color: var(--muted); margin-bottom: 40px; font-size: 1rem; line-height: 1.7; }
  .contact-links { display: flex; flex-direction: column; gap: 16px; }
  .contact-link {
    display: flex; align-items: center; gap: 16px;
    padding: 18px 28px; border-radius: 14px;
    background: rgba(255,255,255,0.04); border: 1px solid var(--glass-border);
    text-decoration: none; color: var(--text);
    transition: all 0.3s; text-align: left;
  }
  .contact-link:hover {
    background: rgba(124,92,252,0.1); border-color: rgba(124,92,252,0.3);
    transform: translateX(4px);
  }
  .contact-icon {
    width: 42px; height: 42px; border-radius: 12px; display: flex; align-items: center; justify-content: center;
    font-size: 1.1rem; flex-shrink: 0;
  }
  .ci-phone { background: linear-gradient(135deg, rgba(67,232,216,0.2), rgba(67,232,216,0.1)); }
  .ci-email { background: linear-gradient(135deg, rgba(124,92,252,0.2), rgba(124,92,252,0.1)); }
  .ci-linkedin { background: linear-gradient(135deg, rgba(252,92,125,0.2), rgba(252,92,125,0.1)); }
  .contact-link-label { font-size: 0.75rem; color: var(--muted); display: block; margin-bottom: 3px; font-weight: 500; letter-spacing: 0.5px; text-transform: uppercase; }
  .contact-link-val { font-size: 0.95rem; font-weight: 600; }

  /* FOOTER */
  footer {
    position: relative; z-index: 1;
    text-align: center; padding: 32px 60px;
    border-top: 1px solid var(--glass-border);
    color: var(--muted); font-size: 0.85rem;
  }
  footer span {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    font-weight: 600;
  }

  /* SCROLL REVEAL */
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  @media (max-width: 900px) {
    nav { padding: 18px 24px; }
    .nav-links { gap: 20px; }
    #hero, #about, #skills, #projects, #contact { padding: 80px 24px; }
    .about-grid { grid-template-columns: 1fr; }
    .projects-grid { grid-template-columns: 1fr; }
    .contact-card { padding: 36px 24px; }
  }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>
<div class="bg-canvas"></div>
<div class="orb orb1"></div>
<div class="orb orb2"></div>
<div class="orb orb3"></div>
<div class="grid-overlay"></div>

<!-- NAV -->
<nav>
  <div class="nav-logo">VT</div>
  <ul class="nav-links">
    <li><a href="#about">About</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-inner">
    <div class="hero-badge">Available for work</div>
    <h1 class="hero-name">Vigneshwaran<br/><span>Theerthagiri</span></h1>
    <p class="hero-title">Full Stack Developer</p>
    <p class="hero-desc">Crafting seamless digital experiences from database to browser — building systems that scale, perform, and delight.</p>
    <div class="hero-cta">
      <a href="#projects" class="btn-primary">View Projects</a>
      <a href="#contact" class="btn-outline">Get In Touch</a>
    </div>
  </div>
  <div class="hero-scroll">
    <div class="scroll-line"></div>
    <span>Scroll</span>
  </div>
</section>

<!-- ABOUT -->
<section id="about">
  <div class="section-header reveal">
    <div class="section-tag">Who I Am</div>
    <h2 class="section-title">About <span>Me</span></h2>
    <div class="section-line"></div>
  </div>
  <div class="about-grid">
    <div class="glass-card about-card reveal">
      <div class="about-text">
        <p>I'm <strong>Vigneshwaran Theerthagiri</strong>, a passionate Full Stack Developer who loves turning complex problems into elegant, efficient solutions.</p>
        <p>With hands-on experience across both frontend and backend technologies, I build end-to-end applications that are robust, scalable, and user-friendly.</p>
        <p>From crafting pixel-perfect UIs to architecting secure server-side systems — I bring ideas to life across the full stack.</p>
      </div>
    </div>
    <div class="about-stats reveal">
      <div class="stat-card">
        <div class="stat-num">4+</div>
        <div class="stat-label">Projects Delivered</div>
      </div>
      <div class="stat-card">
        <div class="stat-num">8+</div>
        <div class="stat-label">Technologies</div>
      </div>
      <div class="stat-card">
        <div class="stat-num">100%</div>
        <div class="stat-label">Commitment</div>
      </div>
      <div class="stat-card">
        <div class="stat-num">∞</div>
        <div class="stat-label">Curiosity</div>
      </div>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section id="skills">
  <div class="section-header reveal">
    <div class="section-tag">What I Use</div>
    <h2 class="section-title">Tech <span>Stack</span></h2>
    <div class="section-line"></div>
  </div>
  <div class="skills-grid">
    <div class="skill-pill reveal"><span class="skill-icon">🐘</span><div class="skill-name">PHP</div><div class="skill-type">Backend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">🌐</span><div class="skill-name">HTML</div><div class="skill-type">Frontend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">🎨</span><div class="skill-name">CSS</div><div class="skill-type">Frontend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">⚡</span><div class="skill-name">JavaScript</div><div class="skill-type">Frontend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">⚛️</span><div class="skill-name">React</div><div class="skill-type">Frontend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">🟢</span><div class="skill-name">Node.js</div><div class="skill-type">Backend</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">🗄️</span><div class="skill-name">MySQL</div><div class="skill-type">Database</div></div>
    <div class="skill-pill reveal"><span class="skill-icon">📊</span><div class="skill-name">SQL</div><div class="skill-type">Database</div></div>
  </div>
</section>

<!-- PROJECTS -->
<section id="projects">
  <div class="section-header reveal">
    <div class="section-tag">What I Built</div>
    <h2 class="section-title">Featured <span>Projects</span></h2>
    <div class="section-line"></div>
  </div>
  <div class="projects-grid">
    <div class="glass-card project-card reveal">
      <div class="project-num">01</div>
      <div class="project-name">CRM System</div>
      <div class="project-desc">Designed and implemented modules for customer data management and analytics — providing businesses with powerful insights and streamlined relationship tracking.</div>
      <div class="project-tags">
        <span class="tag">PHP</span><span class="tag">MySQL</span><span class="tag">JavaScript</span><span class="tag">Analytics</span>
      </div>
    </div>
    <div class="glass-card project-card reveal">
      <div class="project-num">02</div>
      <div class="project-name">VMS — Vivaga Management</div>
      <div class="project-desc">Developed a comprehensive system for managing and tracking vivaga operations — built for reliability, speed, and real-time operational visibility.</div>
      <div class="project-tags">
        <span class="tag">Node.js</span><span class="tag">React</span><span class="tag">MySQL</span><span class="tag">Dashboard</span>
      </div>
    </div>
    <div class="glass-card project-card reveal">
      <div class="project-num">03</div>
      <div class="project-name">Ticket Raising Web App</div>
      <div class="project-desc">Created a web-based application for seamless ticket management and resolution — enabling teams to raise, track, and resolve issues with full transparency.</div>
      <div class="project-tags">
        <span class="tag">PHP</span><span class="tag">HTML/CSS</span><span class="tag">JavaScript</span><span class="tag">SQL</span>
      </div>
    </div>
    <div class="glass-card project-card reveal">
      <div class="project-num">04</div>
      <div class="project-name">Inventory Management</div>
      <div class="project-desc">Engineered an efficient inventory management system for real-time tracking and reporting — reducing stock errors and improving operational efficiency.</div>
      <div class="project-tags">
        <span class="tag">PHP</span><span class="tag">MySQL</span><span class="tag">React</span><span class="tag">Real-time</span>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="section-header reveal">
    <div class="section-tag">Let's Connect</div>
    <h2 class="section-title">Get In <span>Touch</span></h2>
    <div class="section-line"></div>
  </div>
  <div class="contact-inner reveal">
    <div class="glass-card contact-card">
      <div class="contact-title">Ready to build something?</div>
      <p class="contact-subtitle">I'm always open to new opportunities, collaborations, and interesting conversations. Let's create something great together.</p>
      <div class="contact-links">
        <a href="tel:+918124791232" class="contact-link">
          <div class="contact-icon ci-phone">📱</div>
          <div>
            <span class="contact-link-label">Mobile</span>
            <span class="contact-link-val">+91 8124791232</span>
          </div>
        </a>
        <a href="mailto:vickysugu043@gmail.com" class="contact-link">
          <div class="contact-icon ci-email">✉️</div>
          <div>
            <span class="contact-link-label">Email</span>
            <span class="contact-link-val">vickysugu043@gmail.com</span>
          </div>
        </a>
        <a href="https://linkedin.com/in/vicky-sugu-3b5825158" target="_blank" class="contact-link">
          <div class="contact-icon ci-linkedin">💼</div>
          <div>
            <span class="contact-link-label">LinkedIn</span>
            <span class="contact-link-val">linkedin.com/in/vicky-sugu-3b5825158</span>
          </div>
        </a>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <p>Designed & Built by <span>Vigneshwaran Theerthagiri</span> &nbsp;·&nbsp; Full Stack Developer</p>
</footer>

<script>
  // Custom cursor
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');
  let mx = 0, my = 0, rx = 0, ry = 0;
  document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; cursor.style.left = mx+'px'; cursor.style.top = my+'px'; });
  function animRing() {
    rx += (mx - rx) * 0.12; ry += (my - ry) * 0.12;
    ring.style.left = rx+'px'; ring.style.top = ry+'px';
    requestAnimationFrame(animRing);
  }
  animRing();
  document.querySelectorAll('a, button, .skill-pill, .stat-card').forEach(el => {
    el.addEventListener('mouseenter', () => { ring.style.width='52px'; ring.style.height='52px'; ring.style.borderColor='rgba(124,92,252,0.7)'; });
    el.addEventListener('mouseleave', () => { ring.style.width='36px'; ring.style.height='36px'; ring.style.borderColor='rgba(124,92,252,0.5)'; });
  });

  // Scroll reveal
  const obs = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 80);
      }
    });
  }, { threshold: 0.1 });
  document.querySelectorAll('.reveal').forEach(el => obs.observe(el));

  // Nav scroll effect
  window.addEventListener('scroll', () => {
    document.querySelector('nav').style.background = window.scrollY > 50 ? 'rgba(13,11,30,0.85)' : 'rgba(13,11,30,0.6)';
  });
</script>
</body>
</html>
